syntax = "proto3";
package mainservice;

service Master {
    rpc join (JoinRequest) returns (JoinApproval);
}

message JoinRequest {
    string id = 1;
}

message JoinApproval {
    string id = 1;
}
