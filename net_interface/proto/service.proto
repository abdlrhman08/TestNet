syntax = "proto3";
package mainservice;

service TestNet {
    rpc pullJob (Sentinel) returns (Job);
    rpc register(Empty) returns (stream Job);
}

message Empty {}

message Sentinel {
    string d = 1;
}

message Job {
    string payload = 1;
}
